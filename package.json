{
  "name": "listening",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A personal now-playing display and embeddable Last.fm status card.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}

